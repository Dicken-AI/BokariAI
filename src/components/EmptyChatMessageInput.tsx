import BkComposer from './BkComposer';

/**
 * The new-chat composer — the unified Bokari chat bar (identical to the home's),
 * wired to the real chat actions. See BkComposer.
 */
const EmptyChatMessageInput = () => <BkComposer variant="full" autoFocus />;

export default EmptyChatMessageInput;
