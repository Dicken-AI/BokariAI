import BkComposer from './BkComposer';

/**
 * The in-chat follow-up composer — the same unified Bokari chat bar, compact.
 * See BkComposer.
 */
const MessageInput = () => <BkComposer variant="compact" />;

export default MessageInput;
